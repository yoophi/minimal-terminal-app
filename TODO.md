- [x] Start login shell in `$HOME` by default.
- [x] Notify the user when the shell process exits.
- [x] Close the terminal window with `Cmd+W`.
- [x] Quit the app with `Cmd+Q`.
- [x] Open a new terminal window with `Cmd+N`.
